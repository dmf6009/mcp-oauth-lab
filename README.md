# MCP OAuth Lab
Static validator for remote MCP protected-resource and authorization-server metadata.

Production: https://mcpoauth.guagua-alvin.top

Cloudflare Pages: no framework, production branch `main`, build command `exit 0`, output directory `.`.
